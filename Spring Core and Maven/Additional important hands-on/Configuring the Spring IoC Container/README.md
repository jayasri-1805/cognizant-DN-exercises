# Exercise 5: Configuring the Spring IoC Container

This project demonstrates Spring IoC using XML configuration.

## Steps
1. Configure beans in `applicationContext.xml`.
2. Inject `BookRepository` into `BookService` using setter injection.
3. Run `MainApp`.

Expected Output:
```
Book Repository configured successfully.
```
