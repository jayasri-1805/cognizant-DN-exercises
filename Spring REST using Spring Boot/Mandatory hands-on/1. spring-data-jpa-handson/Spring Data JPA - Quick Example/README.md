# ORM Learn - Spring Data JPA

## Steps
1. Create schema using `database.sql`.
2. Update MySQL username/password in `application.properties`.
3. Run:
```
mvn clean install
mvn spring-boot:run
```

Repository structure follows a simple layered architecture:
- model
- repository
- service
- application
